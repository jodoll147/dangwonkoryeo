import styled from 'styled-components';

const BottomContainer = styled.div`
  section {
    position: relative;
    .bottomBox {
      positon: relative;
      margin-bottom: 30px;
      margin-top: 300px;
      .textBox {
        position: relative;
        p {
          position: relative;
          font-family: 'Cafe24';
          text-align: center;
        }
      }
    }
  }
`;

export default BottomContainer;
