import styled from 'styled-components';

const MainContainer = styled.div`
  section {
    position: relative;
    margin: 0 auto;
    width: 940px;
    text-align: center;
  }
`;

export default MainContainer;
